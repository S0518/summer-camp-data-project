# 可视化大屏部署指南

## 环境要求

- Python 3.9+
- Windows / macOS / Linux

## 1. 安装依赖

```bash
pip install streamlit pandas pillow
```

## 2. 跳过首次邮箱提示

```bash
mkdir -p ~/.streamlit
echo '[general]' > ~/.streamlit/credentials.toml
echo 'email = ""' >> ~/.streamlit/credentials.toml
```

## 3. 确保目录结构正确

```
数据探索/
├── dashboard.py          ← 大屏脚本
├── data/
│   └── change.csv        ← 筛选需要（KPI联动）
└── results/              ← 预生成的图表和数据表
    ├── quality/
    ├── 患者基本结构/
    ├── 核心健康指标分布/
    ├── 衍生风险等级/
    ├── 异常值与医学逻辑检查/
    ├── 指标关系分析/
    ├── 疾病类型健康画像/
    ├── 聚类/
    └── 监督学习/
```

> 注意：`dashboard.py` 必须与 `data/`、`results/` 放在同一目录下，脚本会自动检测路径。

## 4. 启动

```bash
cd 数据探索
streamlit run dashboard.py
```

浏览器自动打开 `http://localhost:8501`。

## 5. 常用启动参数

```bash
# 指定端口
streamlit run dashboard.py --server.port 8888

# 禁止自动打开浏览器
streamlit run dashboard.py --server.headless true

# 允许局域网内其他设备访问（大屏演示场景）
streamlit run dashboard.py --server.address 0.0.0.0
```

## 6. 关闭

终端按 `Ctrl+C`，网页即不可访问。

## 7. 常见问题

| 问题 | 解决 |
|---|---|
| 网页拒绝连接 | 确认终端显示 `Uvicorn server started`，检查端口是否被占用 |
| 图片显示"未找到" | 确认 `results/` 目录结构完整，与 `dashboard.py` 同一层级 |
| KPI 数值不变化 | 确认 `data/change.csv` 存在；图表为预生成静态图片，筛选仅影响KPI数字 |
| 中文字体乱码 | 不影响，图表中的中文已渲染在 PNG 内 |
